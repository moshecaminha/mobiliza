{
  "name": "mobiliza-backend",
  "version": "1.0.0",
  "description": "Backend de produção MOBILIZA — Express + PostgreSQL + JWT + APIs sociais",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
        "start": "node migrate.js && node server.js",
    "dev": "node --watch server.js",
    "migrate": "node migrate.js",
    "test": "echo 'no tests yet' && exit 0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "express-rate-limit": "^7.1.5",
    "pg": "^8.11.3",
    "jsonwebtoken": "^9.0.2",
    "dotenv": "^16.3.1"
  },
  "license": "MIT"
}
